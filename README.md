# JWT-Authentication-and-WebSockets-for-real-time-task-updates
Here is a complete, self-contained single-file Python web application built using FastAPI, SQLite (via SQLAlchemy), JWT Authentication, and WebSockets for real-time task updates across connected clients. It embeds a responsive frontend (HTML5/Tailwind CSS/JavaScript).
